= page_title(t(".title"))
= link_to(t(".new"), new_raid_url) if admin?
%br
%br
#raids
  %form{:name => "sform", :action => "", :style => "display:inline"}
  %table.filter-menu
    %tr
      %td{:align => 'right'}
        %label
          Filter on date:
      %td
        = text_field_tag(:query, @query, :autocomplete => 'off')
        = hidden_field_tag(:page, @page)
#dataTable
  = render(:partial => "raids/raids.html.haml", :locals => {:raids => @raids})
