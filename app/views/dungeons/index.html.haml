= page_title(t(".title"))
= link_to(t(".new"), new_dungeon_url) if admin?
%br
%br
#dungeons
  %form{:name => "sform", :action => "", :style => "display:inline"}
  %table.filter-menu
    %tr
      %td{:align => 'right'}
        %label
          Filter on name:
      %td
        = text_field_tag(:query, @query, :autocomplete => 'off')
        = hidden_field_tag(:page, @page)
#dataTable
  = render(:partial => "dungeons.html.haml", :locals => {:dungeons => @dungeons})