= page_title(t(".title"))
%br
%br
#members
  %form{:name => "sform", :action => "", :style => "display:inline"}
  %table.filter-menu
    %tr
      %td{:align => 'right'}
        %label
          Filter on name:
      %td
        = text_field_tag(:query, @query, :autocomplete => 'off')
    %tr
      %td{:align => 'right'}
        %label
          For the last:
      %td
        = select_tag(:since, options_for_select(["8", "4", "2", "1"]))
        weeks
        = hidden_field_tag(:sort_column, @sort_column)
        = hidden_field_tag(:sort_direction, @sort_direction)
#dataTable
  = render(:partial => "members/raid_attending_members.html.haml", :locals => {:members => @members})
