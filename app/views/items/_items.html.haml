= render(:partial => "shared/pagination.html.haml", :locals => {:items => items})
%br
%table#item-table{:width => "100%"}
  %thead
    %tr.rowHeader
      %th{:width => "25%"}
        Name
        = sort_links(Item, "items.name")
      %th
        Boss
      - admin_only do
        %th{:width => "10%"}
          Action
  %tbody
    = render(:partial => 'items/item.html.haml', :collection => items)
