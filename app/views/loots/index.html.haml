= page_title(t(".title"))
= link_to(t(".new"), new_loot_url) if admin?
%br
%br
#loots
  %form{:name => "sform", :action => "", :style => "display:inline"}
  %table.filter-menu
    %tr
      %td{:align => 'right'}
        %label
          Filter on item or member name:
      %td
        = text_field_tag(:query, @query, :autocomplete => 'off')
        = hidden_field_tag(:sort_column, @sort_column)
        = hidden_field_tag(:sort_direction, @sort_direction)
        = hidden_field_tag(:page, @page)
#dataTable
  = render(:partial => "loots/loots.html.haml", :locals => {:loots => @loots, :skip_paginate => false})